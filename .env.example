# 复制本文件为 .env 并填写（.env 已被 .gitignore 忽略，不会提交进仓库）
# 然后执行：docker compose up -d --build

# 访问口令白名单，逗号分隔多个家庭成员口令（务必改掉默认 888888）
ACCESS_CODE=改成你自己的口令

# 令牌签名密钥，填一段随机字符串（可用 `openssl rand -hex 32` 生成）
JWT_SECRET=改成随机字符串
